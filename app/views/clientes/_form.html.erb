<%= form_for(@cliente) do |f| %>
  <% if @cliente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cliente.errors.count, "error") %> prohibited this cliente from being saved:</h2>

      <ul>
      <% @cliente.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <% if Restaurante.all.count != 0 %>
    <div class="field">
      <%= f.label :nome %><br />
      <%= f.text_field :nome %>
    </div>
    <div class="field">
      <%= f.label :idade %><br />
      <%= f.number_field :idade %>
    </div>
      <div class="field">
      <%= f.label :endereco %><br />
      <%= f.number_field :endereco %>
    </div>
    <div class="field">
      <%= f.label :telefone %><br />
      <%= f.number_field :telefone %>
    </div>

    <div class="field">
      <%= f.label :cidade %><br />
      <%= f.number_field :cidade %>
    </div>

      <div class="field">
      <%= f.label :restaurante_id %><br />
        <%= f.collection_select( :restaurante_id, Restaurante.order('nome'), :id, :nome, {:prompt => true}) %>

    </div>
    <div class="actions" >
   <%= submit_tag 'Criar Cliente' %>
    </div>
  <% else %>
  <h2> É necessário criar um restaurate </h2>
  <% end %>
<% end %>
